

/**
 * Executes the function "greeting".
 * @param name - parameter
 * @returns string
 */
function greeting(name: string) {
  return `Hello, ${name}`;
}
const sub = /**
 * Processes the function "sub".
 * @param a - parameter
 * @param b - parameter
 * @returns number
 */
(a: number, b: number) => a - b;